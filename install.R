# Here you have to list, following the example for caret and rpart, all the packages you want to install. 

install.packages(c("recipes", "xgboost", "tidyverse",
                   "janitor", "conflicted",
                   # "embed", "keras",
                   "mgcv"))